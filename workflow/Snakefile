"""Beginning of execution for `snakemake` command."""
configfile: "../config/Mmul_10.yaml"
#include: "rules/relations.smk"
include: "rules/variant_calling.smk"

rule all:
    """Generate target files. This rule runs automatically when Snakefile is run.
    Change the path under input to change the target files generated by snakemake.
    """
    input: expand("{output_path}bam/{sample}.bam", output_path=config["output_path"], sample=SAMPLE_NAMES)
    #input: config["output_path"] + "bam/WGS14785.bam"
    #input: "../Mmul_8/plink/GbS_2020.ped", "../Mmul_8/plink/GbS_2020.map"
    #input: config["output_path2"] + "relatedness/GbS_2020.2.Q", 
    #       config["output_path2"] + "relatedness/GbS_2020.2.P"
